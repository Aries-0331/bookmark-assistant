# @bookmark-assistant/contracts

Shared public TypeScript contracts for Bookmark Assistant packages.

This package contains data shapes used across the extension and server, including saved link items, bookmark sync payloads, Notion mapping descriptors, and ContextItem export types.

It intentionally contains no runtime implementation or deployment-specific configuration.
