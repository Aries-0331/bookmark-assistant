{
  "name": "bookmark-notion-sync",
  "version": "1.0.0",
  "description": "Chrome extension that syncs bookmarks to Notion with AI features",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -F @bookmark-sync/extension dev",
    "dev:server": "pnpm -F @bookmark-sync/server dev",
    "build": "pnpm -F @bookmark-sync/extension build",
    "build:server": "pnpm -F @bookmark-sync/server build",
    "build:all": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "clean:legacy": "bash scripts/cleanup-legacy-dist.sh",
    "start:server": "pnpm -F @bookmark-sync/server start"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.9.2",
    "prettier": "^3.0.0",
    "eslint": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Your Name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/bookmark-notion-sync"
  }
}
